{
  "name": "todo-evolution",
  "version": "1.0.0",
  "description": "A simple, clean todo application built with vanilla JavaScript, HTML, and CSS.",
  "main": "script.js",
  "scripts": {
    "dev": "live-server --port=3000 --host=localhost --no-browser",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
